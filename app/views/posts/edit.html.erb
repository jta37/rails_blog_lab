<%= form_for @post do |f| %>
  <%= f.label :author %>
  <%= f.text_field :author %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.text_area :description %>
  <%= text_field_tag :tags, @post.tags.map(&:name).join(', ') %>
  <%= f.submit "Update" %>
<% end %>
